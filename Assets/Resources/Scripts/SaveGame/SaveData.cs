using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SaveData : MonoBehaviour
{
    /* SaveData needed:
     * Each object placed in the world, with their position and rotation
     * The floor materials
     * Time and date
     * Resource numbers
     * Research States
     * 
     * 
     * 
     */



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
